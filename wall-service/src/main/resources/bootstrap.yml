spring:
  application:
    name: wall-service
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config-service
      name: wall-service
      profile: default
      failFast: true