spring:
  application:
    name: discovery-service
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config-service
      name: discovery-service
      profile: default
      failFast: true