server:
  context-path: /repost
  port: 8000

#discovery:
#  client:
#    serviceUrl:
#      defaultZone: ${DISCOVERY_URL:http://localhost:8761}/discovery/
#  instance:
#    leaseRenewalIntervalInSeconds: 1
#    leaseExpirationDurationInSeconds: 2

ribbon:
  eureka:
    enabled: true