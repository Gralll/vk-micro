spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Gralll/vk-micro-config.git
          searchPaths: dev

server:
  port: 8888

management:
  context-path: /admin
  security:
    enabled: false