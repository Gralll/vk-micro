spring:
  application:
    name: gateway-service
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config-service
      name: gateway-service
      profile: default
      failFast: true